# ==============================
# 必须配置（安全相关）
# ==============================

# 普通登录密码（必须修改）
USER_PASSWORD=please-change-user-password

# 管理员密码（必须修改）
ADMIN_PASSWORD=please-change-admin-password

# Session签名密钥（必须修改为高强度随机字符串）
SESSION_SECRET=please-change-session-secret


# ==============================
# 可选配置
# ==============================

# 统一对外端口（本地/容器/生产都用这一项）
# 服务端优先读取 APP_PORT；若未设置会回退到 PORT（PaaS常用）再回退到 3000
APP_PORT=3000

# 会话最大持续小时数，开启 rolling 后会滑动续期
SESSION_MAX_AGE_HOURS=8

# 本地开发时前端地址（用于CORS）
FRONTEND_ORIGIN=http://localhost:5173

# 数据目录（默认 ./data）
DATA_DIR=./data

# 临时目录（默认 ./temp）
TEMP_DIR=./temp

# 临时目录保留天数
TEMP_RETENTION_DAYS=3
